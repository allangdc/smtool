import { makeStyles } from "@mui/styles";

const purple = "#b13d7c";

export const useStyles = makeStyles(() => ({
  caption: {
    fontWeight: "bold !important",
    color: purple,
    paddingBottom: 5
  },
  textField: {
    width: "100%"
  }
}));
